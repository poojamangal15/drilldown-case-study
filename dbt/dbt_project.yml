name: drilldown_dbt
version: 1.0.0
config-version: 2
profile: drilldown_profile

model-paths: ["models"]
seed-paths: ["data"]

models:
  drilldown_dbt:
    +materialized: table

seeds:
  drilldown_dbt:
    +quote_columns: false
